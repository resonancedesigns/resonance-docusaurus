# Docusaurus Template

This is a generic Docusaurus template for documentation sites. Copy this directory to start a new docs site, then add your own content in `docs/` and customize `docusaurus.config.ts` and `src/css/custom.css` as needed.