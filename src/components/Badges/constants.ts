/** Default badges configuration data
 *
 * This file provides the default badges data used in the Badges component.
 * It imports the data from a JSON file located in the data directory.
 */

// @ts-ignore
import * as jsonData from '../../../data';

// @ts-ignore
export const DEFAULT_BADGES_DATA = jsonData.badges;
