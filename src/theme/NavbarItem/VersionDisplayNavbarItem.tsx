import React from 'react';
import VersionDisplay from '../../components/VersionDisplay';

const VersionDisplayNavbarItem: React.FC = () => {
  return <VersionDisplay />;
};

export default VersionDisplayNavbarItem;
